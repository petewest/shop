//currency_new is the li with the form new_currency
jQuery("#currency_list").append("<%= escape_javascript(render @currency) %>");
<%= render 'shared/refresh_flash' %>
<%= render 'shared/hide_modal' %>
