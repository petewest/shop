<% provide(:title, "Product list") %>

<% provide(:heading, "Product list") %>


<%= link_to 'New', new_product_path, class: "btn btn-default" %>

<%= render partial: 'product', collection: @products %>

<%= will_paginate %>

