jQuery("#<%= dom_id(@product, :stock_level) %>").replaceWith("<%= escape_javascript(render 'stock_levels/product_stock_level', product: @product) %>");
<%= render 'shared/refresh_flash' %>
<%= render 'shared/hide_modal' %>
