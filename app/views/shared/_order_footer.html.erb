</div>
<%= render 'shared/postage_cost', postage_cost: order.postage_cost if order %>
<div class="row">
	<div class="cart_product">
	</div>
	<div class="cart_actions">
	</div>
	<div class="cart_line_item text-right">
		<%= "Total cost:" if order %>
	</div>
	<div class="cart_line_cost text-right">
		<%= cost_to_currency(order) if order %>
	</div>
	<div class="clearfix"></div>
</div>
</div>
