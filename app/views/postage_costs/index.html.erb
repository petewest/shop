<% provide(:title, "Postage costs") %>

<%= content_tag(:h1, "Postage costs") %>

<table class="table table-striped table-condensed">
	<thead>
		<tr>
			<th>From weight</th>
			<th>To weight</th>
			<th>Cost</th>
			<th>Edit</th>
		</tr>
	</thead>
	<tbody>
		<%= render @postage_costs %>
	</tbody>
</table>

<%= link_to "New", new_postage_cost_path, class: "btn btn-default" %>