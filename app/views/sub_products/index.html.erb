<% provide(:title, "#{@product.name} sub products") %>

<%= content_tag(:h1, "#{@product.name} sub products") %>

<%= link_to "New", new_product_sub_product_path(@product), class: "btn btn-default" %>

<ul>
<%= render @sub_products %>
</ul>