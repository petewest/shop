<% @modes.each do |mode,v| %>
jQuery("#<%= mode %>").replaceWith("<%= escape_javascript( render 'address_wrapper', address: @cart.send(mode.gsub("_attributes","")), mode: mode ) %>");
<% end %>
<%= render 'shared/refresh_flash' %>
