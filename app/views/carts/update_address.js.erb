<% @modes.each do |mode,v| %>
<% actual_mode=mode.to_s.gsub("_attributes","").to_sym %>
jQuery("#<%= actual_mode %>").replaceWith("<%= escape_javascript( render 'address_wrapper', address: @cart.send(actual_mode), mode: actual_mode ) %>");
<% end %>
<%= render 'shared/refresh_flash' %>
